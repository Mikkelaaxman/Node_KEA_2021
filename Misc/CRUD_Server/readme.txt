
Notes:

Scripts can be run with npm run *scriptname* 

See all Environment variable in powershell with:  Get-ChildItem Env:

Client-Server model: Client sends a request, Server sends a response

Use express.static to tell the client what directories are public to access

